// Legal pages entry. Only needs the sticky header state from nav.js —
// there are no reveal animations or product visuals on document pages.
import { initNav } from './nav.js';

window.addEventListener('DOMContentLoaded', () => {
  initNav();
});
